{
  "name": "{{MARKETPLACE_NAME}}",
  "description": "{{PRODUCT}} — internal URLs for prototypes and agent-made files.",
  "owner": {
    "name": "{{PRODUCT}}",
    "url": "{{MARKETPLACE_URL}}"
  },
  "metadata": {
    "description": "{{PRODUCT}} — internal URLs for prototypes and agent-made files."
  },
  "plugins": [
    {
      "name": "{{PLUGIN_NAME}}",
      "source": "./plugins/{{PLUGIN_NAME}}",
      "skills": ["./skills/{{SKILL_NAME}}"],
      "description": "Internal URLs for prototypes, markdown, and agent-made files so work can leave a session.",
      "version": "{{VERSION}}",
      "homepage": "{{ORIGIN}}",
      "keywords": ["{{SKILL_NAME}}", "publish", "preview", "static"]
    }
  ]
}
